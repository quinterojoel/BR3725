def multiply(n):
    #Add the code and an explanation on how to 
    #multiply a number by 2 using bitshift
    #Convert to binary
    #Divide by 2 to find remainders
    #Print remainders into a list
    #Append a 0 at the end

list = []
n = 13
while n is not 0 or 1:
    n = n / 2
list.append(n)
list.append(0)

    return n
